1. Of all the values in the items list, only one of them was wrong. Which value was incorrect? Why were all the other values still correct?
    457 was wrong as the current for loop did not account for integers bigger then 2 digits

2. What was going wrong when iterating through the symbols list?
    It had extra spaces after the formatting, changed to nicer formatting that is horizontal not vertical and all is fixed

3. How did stepping through the code and using the debugger help you find the issues?
    It didn't, error clearly was within the string as shown by error when running it, then just went and did new sorting system as old one was not great

4. Pick 3 issues that you fixed based on the new code analysis rules - how did they improve your code?
    - Cleaned up string formatting
    - Made more efficient sorting system
    - Fixed formatting errors

5. Pick 3 issues that you decided not to fix based on the new code analysis rules - why didn't they need to be fixed?
    - Stuff to do with internal vs external class and it not being set right. I dont see a reason why we need to use internal classes as the information is not important and don't matter if it is accessed elsewhere